{
  "name": "leave-by",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "iPhone home-screen PWA: flight number + date → leave-the-house time.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.mjs",
    "icons": "python3 scripts/generate-icons.py"
  },
  "engines": {
    "node": ">=18"
  }
}
